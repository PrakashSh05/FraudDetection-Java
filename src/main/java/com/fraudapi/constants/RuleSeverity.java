package com.fraudapi.constants;

/**
 * Enumeration representing the severity classification of a fraud rule.
 */
public enum RuleSeverity {

    /** Low severity rule trigger. */
    LOW,

    /** Medium severity rule trigger. */
    MEDIUM,

    /** High severity rule trigger. */
    HIGH,

    /** Critical severity rule trigger. */
    CRITICAL
}
